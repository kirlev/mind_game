<% provide(:title, "התחברות") %>
<h1>התחבר</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(:session, url: sessions_path) do |f| %>

      <%= f.label "שם משתמש" %>
      <%= f.text_field :username %>

      <%= f.label "סיסמה" %>
      <%= f.password_field :password %>

      <%= f.submit "התחבר", class: "btn btn-large btn-primary" %>
    <% end %>

    <p>לא רשום?<%= link_to "הרשם עכשיו!", signup_path %></p>
  </div>
</div>
